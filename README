🚀 YaniStat (Projet Bourse Skins CS)
Projet de site web pour suivre les prix et l'historique des skins Counter-Strike, en utilisant l'API du marché Steam et une base de données Supabase.

🖥️ Comment lancer le projet
Ce projet a deux parties : le site web (frontend) que les utilisateurs voient, et le script de remplissage (backend) qui récupère les prix.

1. Afficher le site web (Frontend)
C'est la partie index.html et main.js.

Ouvrez le dossier du projet dans Visual Studio Code.

Assurez-vous d'avoir l'extension Live Server.

Faites un clic droit sur le fichier index.html.

Choisissez "Open with Live Server".

Votre navigateur s'ouvrira sur une adresse locale (ex: http://127.0.0.1:5500).

2. Mettre à jour les prix (Script Backend)
Le site web ne fait que lire la base de données. Pour la remplir, vous devez lancer ce script.

Ouvrez un terminal dans VS Code.

Installez les dépendances (vous n'avez besoin de le faire qu'une seule fois) :

Bash

npm install @supabase/supabase-js
Configurez vos clés secrètes :

Ouvrez le fichier remplir_la_db.js.

Remplacez VOTRE_CLE_SERVICE_ROLE_SECRETE_ICI par votre vraie clé service_role de Supabase.

⚠️ ATTENTION : Ne partagez jamais ce fichier publiquement avec la clé à l'intérieur !

Lancez le script :

Bash

node remplir_la_db.js
Le script va contacter l'API Steam, puis mettre à jour vos tables Supabase items et price_history.

🛠️ Technologies utilisées
Frontend :

HTML5

CSS3 (style de base)

JavaScript (ES6+)

Backend (Script) :

Node.js

Base de données :

Supabase (Base de données PostgreSQL hébergée)

Bibliothèques (Libs) :

Chart.js : Pour l'affichage des graphiques d'historique.

supabase-js : Client officiel pour interagir avec Supabase depuis JavaScript.

🔗 Sources des données (API)
API Steam Community Market (Non-officielle) :

https://steamcommunity.com/market/priceoverview/

Cet endpoint est utilisé par le script remplir_la_db.js pour récupérer le prix le plus bas et le volume actuel d'un item.

🗃️ Structure de la base de données (Supabase)
Table : items
Stocke la liste des skins et leur dernier prix connu. C'est ce qui est utilisé pour afficher la liste de gauche.

name (text, Unique) : Nom du skin (ex: "AK-47 | Redline...")

price (numeric) : Le dernier prix relevé.

volume (int4) : Le dernier volume relevé.

last_updated (timestamptz) : Date de la dernière mise à jour.

Table : price_history
Stocke l'historique des prix que nous construisons nous-mêmes. Chaque fois que le script remplir_la_db.js est lancé, il ajoute une nouvelle ligne ici pour chaque item.

name (text) : Nom du skin (pour le lier à la table items).

price (numeric) : Le prix à ce moment-là.

timestamp (timestamptz, default: now()) : Date et heure de l'enregistrement.
